# Build output
target/
*.class
*.jar
*.war

# Maven
.mvn/wrapper/maven-wrapper.jar

# IDE — IntelliJ IDEA
.idea/
*.iml
*.iws
*.ipr

# IDE — VS Code
.vscode/

# IDE — Eclipse
.project
.classpath
.settings/

# OS noise
.DS_Store
Thumbs.db

# Local config overrides — never commit secrets
src/main/resources/application-local.yml
src/main/resources/application-local*.yml
application-local.yml
.env
.env.*

# Maven debug / scratch files
cp.txt
dependency-reduced-pom.xml

# Reference docs and Claude Code state — keep these out of the project repo
# (agentscope_docs_v2/ is intentionally NOT ignored — v2 文档归档进仓库)
agentscope-java-docs/
.claude/

# v2 draft code (AgentScope 2.0.0-RC2 not stable yet; kept on disk for
# future upgrade reference, but excluded from the v1 build tree so it
# can never be picked up by maven scanning or pollute classpath).
src/main/java/io/

# H2 local database — regenerable
data/

# Node deps (mock third-party backend)
examples/node_modules/

# MCP / IDE artifacts
.codegraph/
.cursor/
.mcp.json

# daemon pid files
*.pid
**/node_modules/

io/**
